# ================================
# SecureRAG Backend - Dependencies
# ================================

# --- Web Framework ---
fastapi==0.115.0          # Modern, fast web framework for building APIs
uvicorn[standard]==0.30.6 # ASGI server to run FastAPI

# --- LangChain (RAG Pipeline) ---
langchain==0.3.0           # LangChain core - orchestrates LLM + retrieval
langchain-anthropic==0.2.0 # LangChain integration for Claude (Anthropic)
langchain-community==0.3.0 # Community integrations (FAISS, loaders, etc.)

# --- Vector Store ---
faiss-cpu==1.9.0.post1           # Facebook AI Similarity Search - local vector DB

# --- Document Processing ---
pypdf==4.3.1               # Parse and extract text from PDF files
python-docx==1.1.2         # Parse .docx (Word) documents
python-multipart==0.0.9    # Required for FastAPI file upload support

# --- Embeddings ---
sentence-transformers==3.0.1  # Local embeddings model (no API cost!)

# --- Authentication ---
python-jose[cryptography]==3.3.0  # JWT token creation and verification
passlib[bcrypt]==1.7.4            # Secure password hashing

# --- Database ---
sqlalchemy==2.0.32         # ORM - works with PostgreSQL, SQLite, etc.
alembic==1.13.2            # Database migration tool
# psycopg2-binary==2.9.9     # PostgreSQL adapter (swap to SQLite for dev)

# --- Config & Env ---
pydantic==2.8.2            # Data validation (used heavily in FastAPI)
pydantic-settings==2.4.0   # Load settings from .env file
python-dotenv==1.0.1       # Load .env variables

# --- Monitoring & Logging ---
loguru==0.7.2              # Beautiful, structured logging

# --- Dev Tools ---
pytest==8.3.2              # Testing framework
pytest-asyncio==0.23.8     # Async test support
httpx==0.27.0              # Async HTTP client (used in tests)
